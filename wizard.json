{
  "pages": [
    {
      "id": "cli",
      "title": "AI Coding Assistant",
      "description": "Which CLI should run inside the container?",
      "fields": [
        { "id": "CODING_CLI",      "type": "single_select", "source": "catalog.clis", "default": "claude" },
        { "id": "CONTAINER_NAME",  "type": "text",          "prompt": "Container name (shown in docker ps):", "default": "codetainyrrr" }
      ]
    },
    {
      "id": "paths",
      "title": "Project Directory",
      "description": "The folder on your host machine mounted as /workspace inside the container.",
      "hint": "Both Windows (C:\\path) and Unix (/path or C:/path) formats accepted.",
      "fields": [
        { "id": "PROJECT_DIR",        "type": "path",      "prompt": "Path to your project (absolute path):",    "default": "" },
        { "id": "EXTRA_WORKSPACES",   "type": "path_list", "prompt": "Extra paths (semicolon-separated):",       "default": "", "condition_field": "_extra_ws" }
      ]
    },
    {
      "id": "claude_settings",
      "title": "Claude Settings",
      "description": "Share project memories with Claude Desktop?",
      "hint": "Yes — projects/ and todos/ sync with your host Claude Desktop. No — fully isolated.",
      "condition": "${CODING_CLI} == 'claude'",
      "fields": [
        { "id": "CLAUDE_DIR",   "type": "path", "prompt": "Path to your Claude config dir:", "default": "", "required": false },
        { "id": "CLAUDE_JSON",  "type": "path", "prompt": "Path to your claude.json file:",  "default": "", "required": false }
      ]
    },
    {
      "id": "api_keys",
      "title": "API Keys",
      "description": "Keys are stored only in your local .env — never sent anywhere.",
      "auto_keys": true,
      "fields": [
        { "id": "ANTHROPIC_API_KEY",   "type": "secret", "prompt": "Anthropic API key:",   "hint": "blank = keep existing or use OAuth login", "required": false },
        { "id": "OPENAI_API_KEY",      "type": "secret", "prompt": "OpenAI API key:",      "default": "", "required": false },
        { "id": "OPENROUTER_API_KEY",  "type": "secret", "prompt": "OpenRouter API key:",  "default": "", "required": false },
        { "id": "GEMINI_API_KEY",      "type": "secret", "prompt": "Gemini API key:",      "default": "", "required": false }
      ]
    },
    {
      "id": "git_identity",
      "title": "Git Identity",
      "description": "Used for commits made inside the container.",
      "fields": [
        { "id": "GIT_AUTHOR_NAME",   "type": "text", "prompt": "Your name:",  "default": "" },
        { "id": "GIT_AUTHOR_EMAIL",  "type": "text", "prompt": "Your email:", "default": "" }
      ]
    },
    {
      "id": "tools",
      "title": "Dev Tools  (optional)",
      "description": "Lazy-installed on first run into named volumes — instant on every start after.",
      "fields": [
        { "id": "INSTALL_TOOLS", "type": "multiselect", "source": "catalog.tools" }
      ]
    },
    {
      "id": "plugins",
      "title": "Plugins  (optional)",
      "description": "Installed once, sentineled. Add/remove later: codetainyrrr plugins add/remove <name>",
      "fields": [
        { "id": "INSTALL_PLUGINS", "type": "multiselect", "source": "catalog.plugins" }
      ]
    },
    {
      "id": "custom_configs",
      "title": "Custom Configs  (optional)",
      "description": "Point to files on your host to seed your container's editable copies. Blank = use the project's baked defaults.",
      "hint": "Files are copied into the container on first start, then edited inside the container only. Your host files are never touched after that.",
      "fields": [
        { "id": "CCSTATUSLINE_CONFIG", "type": "path", "prompt": "ccstatusline settings.json path (blank = built-in default):", "default": "", "required": false },
        { "id": "ZSH_EXTRA_CONFIG",    "type": "path", "prompt": "Extra zsh config to source at shell start (blank = built-in default):", "default": "", "required": false },
        { "id": "STARSHIP_CONFIG",     "type": "path", "prompt": "starship.toml path (blank = built-in default):", "default": "", "required": false }
      ]
    }
  ]
}
