# Extra zsh config sourced by ~/.zshrc inside the codetainyrrr container.
# Edit this file freely — your edits live in the container's home volume
# and never touch the host filesystem.
#
# Examples:
#   alias gs='git status'
#   alias ll='ls -la'
#   export PAGER='less -R'
#   bindkey -e
