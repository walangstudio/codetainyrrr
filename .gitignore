# Secrets — actual environment, never the template
.env
.env*
!env.example

# User-local catalog additions — gitignored, mounted into container by `codetainyrrr run`
catalog.user.json

# Default project mount (your code lives here, not the container's)
workspace/

# OS detritus
.DS_Store
Thumbs.db
desktop.ini

# Editor swap / backup files
*.swp
*.swo
*~
.vscode/
.idea/

# Logs and temp
*.log
tmp/
temp/
*.tmp

# Local build/test artifacts
*.tgz
*.tar.gz

# Rust build output
target/
# Keep Cargo.lock for reproducible builds (binary crate)

